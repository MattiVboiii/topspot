# pnpm 11+ project config (single package)
packages:
  - "."

# Explicitly allow/deny dependency install scripts (required by pnpm 11)
allowBuilds:
  "@firebase/util": true
  protobufjs: true
  sharp: true
  unrs-resolver: true
